#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdef.h>
#include <string.h>

int _putchar(char c);
int _printf(const char *format, ...);
int _funcopr(const char *format, prf opr[], va_list list);

#endif
